<UserControl x:Class="Labb_3_Quiz.Views.PlayQuizView"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="160"/>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.ColumnSpan="2" Grid.Row="0" Padding="5" Margin="5" Height="400" Width="600" >
            <Image x:Name="QuestionImage" Stretch="Uniform"/>
        </Border>


        <Border Grid.ColumnSpan="2" Grid.Row="1">
            <TextBlock  x:Name="QuestionText" Text="{Binding CurrentQuestion.Statement}" TextWrapping="Wrap" FontSize="40" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Column="0" Grid.Row="2" Padding="5" Margin="5">
            <Button x:Name="AnswerOneButton" Content="{Binding CurrentQuestion.Answers[0]}" FontSize="25"  Click="AnswerOne_Click"/>
        </Border>

        <Border Grid.Column="1" Grid.Row="2" Padding="5" Margin="5">
            <Button x:Name="AnswerTwoButton" Content="{Binding CurrentQuestion.Answers[1]}" FontSize="25" Click="SubmitTwo_Click"/>
        </Border>

        <Border Grid.Column="0" Grid.Row="3" Padding="5" Margin="5">
            <Button x:Name="AnswerThreeButton" Content="{Binding CurrentQuestion.Answers[2]}" FontSize="25" Click="AnswerThree_Click"/>
        </Border>

        <Border Grid.Column="1" Grid.Row="3" Padding="5" Margin="5">
            <Button x:Name="AnswerFourButton" Content="{Binding CurrentQuestion.Answers[3]}" FontSize="25" Click="AnswerFour_Click"  />

        </Border>
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center"> 
            <TextBlock Text="Score:" FontSize="30" />
            <TextBlock x:Name="ScoreTextBlock" FontSize="30" />
        </StackPanel>
    </Grid>

</UserControl>
