<UserControl x:Class="Labb_3_Quiz.Views.PlayQuizView"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="160"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <Border Grid.ColumnSpan="2" Grid.Row="0" Padding="5" Margin="5" Height="400" Width="600" >
            <Image x:Name="QuestionImage" Stretch="Uniform"/>
        </Border>


        <Border Grid.ColumnSpan="2" Grid.Row="1">
            <TextBlock x:Name="Questionblock" Text="{Binding CurrentQuestion.Statement}" FontSize="40" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Column="0" Grid.Row="2" Padding="5" Margin="5">
            <Button x:Name="AnswerOneButton" Content="{Binding CurrentQuestion.Answers[0]}" FontSize="50"  Click="SubmitAnswer1"/>
        </Border>

        <Border Grid.Column="1" Grid.Row="2" Padding="5" Margin="5">
            <Button x:Name="AnswerTwoButton" Content="{Binding CurrentQuestion.Answers[1]}" FontSize="50" Click="SubmitAnswer2"/>
        </Border>

        <Border Grid.Column="0" Grid.Row="3" Padding="5" Margin="5">
            <Button x:Name="AnswerThreeButton" Content="{Binding CurrentQuestion.Answers[2]}" FontSize="50" Click="SubmitAnswer3"/>
        </Border>

        <Border Grid.Column="1" Grid.Row="3" Padding="5" Margin="5">
            <Button x:Name="AnswerFourButton" Content="{Binding CurrentQuestion.Answers[3]}" FontSize="50" Click="SubmitAnswer4"  />

        </Border>

    </Grid>

</UserControl>
